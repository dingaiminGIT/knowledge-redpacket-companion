# 0.4.0-kotlin-rc1 验收记录

## 范围与发布边界

- 从 `main` 的 `86b663c` 创建独立分支 `codex/kotlin-refactor`。
- 13 个应用源文件、5 个测试文件迁移至 Kotlin；应用模块不再包含 Java 源文件。
- Kotlin Android 插件 2.1.21，保留 AGP 8.8.2、Gradle 8.10.2、JDK 17。
- 包名和签名保持兼容，versionCode 12，可覆盖升级并保留缓存。
- 本轮优先保持行为兼容；没有重做 UI 框架或更换官方播放接口。后续可再拆分大类。
- 这是临时验收包，不是正式发布。用户验收前不合并 main，不替换网站下载。

## 2026-09-10 USB 真机回归

设备：小米 2410DPN6CC，Android SDK 36 / HyperOS。

- 覆盖安装成功，旧缓存 25 条与自定义排序保留，启动无崩溃。
- 下一条进入官方播放器实际 PLAYING；暂停后保持 PAUSED。
- 管理页移除非当前条目，当前暂停内容不受影响；强制停止伴侣再启动，移除仍保留。
- 恢复移除内容成功；拖动将 009 与 010 对调，随后恢复测试前顺序。
- 重新同步通过得到登录态确认 25 条有效红包，状态为 authenticated_bridge / complete。
- 接近结尾 3 分钟处开始测试，07:57:38 息屏；08:00:27.151 检测播完，
  08:00:27.443 下一条 PLAYING，确认耗时 292 ms。08:00:53 检查仍为 Dozing，
  从「012 山海经」进入「05｜内驱」，伴侣进程未冻结，唤醒锁保持有效。
- 测试未清除用户数据、未卸载应用，也未修改得到账号内容。

## 构建验证

- `./gradlew testDebugUnitTest assembleDebug lintDebug` 全部通过。
- 27 项单元测试通过（原 24 项迁移，增加 3 项 Kotlin 空值及排序回归测试）。
- Lint 无错误；保留现有 API 弃用、同步偏好写入、可访问性等警告，未宣称零警告。
- 最终 APK SHA-256：`3d96bdb74cabf689681a18a2012207d7f4444dff4a7ca4047e6138adda0984ce`。

## 解释与限制

此前 Java 版息屏失败由 HyperOS Greezer 冻结伴侣复现；开启伴侣自启动后通过对照测试。
本轮继续保留该自启动和无限制省电设置。Kotlin 本身并不绕过系统后台策略。
测试只覆盖当前手机及有限时长；切换耗时表示 MediaSession 状态确认，不是音频无缝间隙测量。
